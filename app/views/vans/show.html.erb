<div class="container">
<h2><%= @van.model%></h2>
<p>Fuel: <%= @van.fuel %></p>
<p>Price: <%= @van.price %></p>
<p><%= @van.surface %></p>
<%= link_to "Book this van",  new_van_booking_path(@van), class: "btn btn-flat" %>

<%= link_to "Back to all vans", vans_path %>
</div>
